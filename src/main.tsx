import React from 'react'
import ReactDOM from 'react-dom/client'
import DividendApp from './DividendApp.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DividendApp />
  </React.StrictMode>,
)

